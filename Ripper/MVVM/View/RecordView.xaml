<UserControl x:Class="Ripper.MVVM.View.RecordView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Ripper.MVVM.View"
             mc:Ignorable="d" d:DesignHeight="75" d:DesignWidth="390">
    <Border Grid.Column="0" CornerRadius="15" BorderBrush="White"
            BorderThickness="1" Background="#191919" Margin="5"
            Height="65">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition Width="33"/>
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <Border Margin="2" CornerRadius="15" Grid.RowSpan="3">
                <Border.Background>
                    <ImageBrush Stretch="UniformToFill" ImageSource="YouTube.jpg" x:Name="Thumbnail"/>
                </Border.Background>
            </Border>

            <TextBlock VerticalAlignment="Center" HorizontalAlignment="Left"
                       Foreground="White" Grid.Column="1" Grid.ColumnSpan="3"
                       Margin="5,0,0,0" Text="Retrieving..." x:Name="Title"/>

            <TextBlock VerticalAlignment="Center" HorizontalAlignment="Left"
                       Foreground="White" Grid.Column="1" Grid.Row="1"
                       Margin="5,0,0,0" Text="Retrieving..." x:Name="Author"/>

            <TextBlock VerticalAlignment="Center" HorizontalAlignment="Left"
                       Foreground="White" Grid.Column="2" Grid.Row="1"
                       Margin="5,0,0,0" Text="--:--:--" x:Name="Length"/>

            <TextBlock VerticalAlignment="Center" HorizontalAlignment="Left" 
                       Foreground="White" Grid.Column="3" Grid.Row="1" 
                       Margin="5,0,0,0" Text="Retrieving..." x:Name="Status"/>

            <ProgressBar Margin="5,0,5,0" Name="PBar" Grid.Column="1" Grid.Row="2" Height="5"
                         VerticalAlignment="Center"  Grid.ColumnSpan="3" IsIndeterminate="True"/>

            <Border Grid.Column="4" CornerRadius="15" Height="30" Margin="-1"
                    BorderBrush="White" BorderThickness="1" HorizontalAlignment="Right"
                    VerticalAlignment="Top"  Grid.RowSpan="2" Width="30">
                <Button Grid.Column="3" FontFamily="Segoe MDL2 Assets" Background="#191919"
                        Content="&#xE10A;" BorderThickness="0" Foreground="White">
                    <Button.Resources>
                        <Style TargetType="{x:Type Border}">
                            <Setter Property="CornerRadius" Value="15"/>
                        </Style>

                        <Style TargetType="{x:Type Button}">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type Button}">
                                        <Border Background="{TemplateBinding Background}">
                                            <ContentPresenter HorizontalAlignment="Center"
                                                              VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#3D3D3D"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Resources>
                </Button>
            </Border>
        </Grid>
    </Border>
</UserControl>
